specVersion: "v1beta"
serviceId: "exam-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "exam-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
